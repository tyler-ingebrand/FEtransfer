# TransferLearningAIStats

## Installation
```commandline
pip install torch torchvision torchaudio 
pip install FunctionEncoder==0.1.0 numpy matplotlib tqdm tensorboard
pip install gymnasium[mujoco]
```